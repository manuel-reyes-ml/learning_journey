> ⚠️ **Harness note.** You cannot run a local model here, so you cannot *prove* the
> judge ran locally — you can only read what the suite configures and report it.
> Treat a missing or cloud judge on AFC / Crucible as 🔴 rather than assuming.
